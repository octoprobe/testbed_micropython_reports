# Minimal Redis config loaded explicitly to avoid startup warnings.
# Keep foreground mode because process lifecycle is managed by start_uvicorn.sh.
port 6379
bind 127.0.0.1
protected-mode yes
daemonize no
loglevel warning
